---
title: Button
description: A button element that can act as a link or trigger an action.
---

import ButtonDemo from "../../../../components/docs/ButtonDemo.vue";

<span style="display:inline-flex;margin-bottom:16px;padding:1px 8px;border-radius:9999px;font-size:11px;font-weight:500;background:#DCFCE7;color:#15803D;">Ready</span>

A button triggers an action — submitting a form, navigating to a page, or executing a callback. Mizu uses Nuxt UI's `UButton` with custom sizing (28/36/40px) and typography.

## Examples

### Common Patterns
Real-world button combinations showing the recommended pairing of color, variant, and icon for common actions in both md and sm sizes.

<ButtonDemo section="patterns" client:only="vue" />

### Sizes
Mizu overrides three sizes: sm (28px), md (36px), and lg (40px).

<ButtonDemo section="sizes" client:only="vue" />

### Colors
Eight semantic colors including Mizu custom families nexus and ycp.

<ButtonDemo section="colors" client:only="vue" />

### Variants
Four core variants: solid, outline, ghost, and link. Soft and subtle are also available via Nuxt UI but are not part of the core Mizu design system.

<ButtonDemo section="variants" client:only="vue" />

### With Icons
Icons can appear on the leading side, trailing side, or both. Icon-only buttons auto-square and share the same height as labeled buttons.

<ButtonDemo section="icons" client:only="vue" />

### Disabled
Prevent interaction with the disabled attribute. Disabled buttons have reduced opacity and no pointer events.

<ButtonDemo section="disabled" client:only="vue" />

### Loading
Show a loading spinner to indicate an in-progress action. Supports a custom icon via the loading-icon prop.

<ButtonDemo section="loading" client:only="vue" />

### Link
Render as an anchor tag using to (router) or href (URL). The link variant applies a dashed underline with tight offset.

<ButtonDemo section="link" client:only="vue" />

### Block & Square
Block fills the full container width. Square gives equal horizontal and vertical padding for icon-only buttons.

<ButtonDemo section="block" client:only="vue" />

### Label Prop
Use the label prop as a shorthand for simple text-only buttons instead of the default slot.

<ButtonDemo section="label" client:only="vue" />

### Avatar
Embed an avatar inside a button for user profile actions. Control the avatar size independently from the button size.

<ButtonDemo section="avatar" client:only="vue" />

## API

Reference: [Nuxt UI Button](https://ui.nuxt.com/docs/components/button)

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl'` | `'md'` | Button size. Mizu overrides sm/md/lg to 28/36/40px |
| `color` | `'primary' \| 'secondary' \| 'nexus' \| 'ycp' \| 'success' \| 'error' \| 'warning' \| 'info' \| 'neutral'` | `'primary'` | Semantic color |
| `variant` | `'solid' \| 'outline' \| 'soft' \| 'subtle' \| 'ghost' \| 'link'` | `'solid'` | Visual style |
| `icon` | `string` (Iconify) | — | Icon from Iconify (e.g. `'i-ph-magnifying-glass'`) |
| `trailing` | `boolean` | `false` | When `true`, icon displays on the right |
| `loading` | `boolean` | `false` | Show a loading spinner instead of the icon |
| `disabled` | `boolean` | `false` | Disables interaction |
| `block` | `boolean` | `false` | Full-width button |
| `square` | `boolean` | `false` | Equal padding on all sides |
| `to` / `href` | `string \| RouteLocation` | — | Render as a link |
| `type` | `'button' \| 'submit' \| 'reset'` | `'button'` | Native button type |

### Slots

| Slot | Props | Description |
|------|-------|-------------|
| `default` | — | Button label/content |
| `leading` | `{ ui }` | Custom leading content (before label) |
| `trailing` | `{ ui }` | Custom trailing content (after label) |

## Theme

Mizu overrides Nuxt UI's button sizing to match the Mizu design spec. Consumers apply the theme via `@mizu-ui/vue/theme`:

```ts
// app.config.ts
import { mizuTheme } from "@mizu-ui/vue/theme";
export default defineAppConfig({ ui: mizuTheme });
```

### Design Specs

| Size | Height | H-Padding | V-Padding | Gap | Radius | Font | Icon |
|------|--------|-----------|-----------|-----|--------|------|------|
| **SM** | 28px | 8px | 8px | 4px | 4px (`rounded`) | 10px | 10px (`size-2.5`) |
| **MD** | 36px | 10px | 10px | 6px | 6px (`rounded-md`) | 12px (`text-xs`) | 16px (`size-4`) |
| **LG** | 40px | 10px | 10px | 8px | 6px (`rounded-md`) | 14px (`text-sm`) | 20px (`size-5`) |

### Color Map

Mizu maps its design tokens to Nuxt UI color slots:

| Mizu Token | Nuxt UI Color |
|------------|---------------|
| `--color-hub-primary` | `primary` |
| `--color-hub-secondary` | `secondary` |
| `--color-scale-nexus-500` | `nexus` |
| `--color-feedback-success-base` | `success` |
| `--color-feedback-error-base` | `error` |
| `--color-feedback-warning-base` | `warning` |
| `--color-feedback-info-base` | `info` |

### Variant Map

Nuxt UI uses a different variant naming convention than the old MizuButton:

| Old Mizu | Nuxt UI | Description |
|----------|---------|-------------|
| `primary` | `solid` + `color="primary"` | Filled background |
| `accent` | `solid` + `color="secondary"` | Secondary filled |
| `ghost` | `ghost` | Transparent, hover state |
| `outline` | `outline` | Border only |

### Typography

| Property | Value |
|----------|-------|
| Font Family | DM Sans (`--font-family-sans`) |
| Font Weight | 500 (medium) |
| Text Transform | None |
| Letter Spacing | Normal |

### Transitions

Mizu overrides Nuxt UI button transitions with `transition-all duration-300` using the `ease-emphasized` curve for smooth hover, focus, and active state animations.

## Anatomy

A button is a single element:

| Part | Required | Description |
|------|----------|-------------|
| Root | Yes | `<button>` or `<a>` element with styling |

With optional leading icon, label, and trailing icon inline.

## Implementation Notes

1. **Use `<UButton>` directly** — No Mizu wrapper needed. Apply the theme preset and Nuxt UI handles the rest.
2. **Mizu sizing is opt-in via theme** — Consumers must import `@mizu-ui/vue/theme` and merge into `app.config.ts`. Without it, Nuxt UI defaults apply.
3. **Icon naming** — Uses Iconify format. Mizu's default collection is Phosphor (`i-ph-*`); other collections like Lucide (`i-lucide-*`) and Heroicons (`i-heroicons-*`) work too. See the [Icons guide](/getting-started/icons) for the full reference.
4. **`leadingIcon` / `trailingIcon`** — The theme preset also overrides icon sizes per button size variant.
5. **Loading state** — Set `loading` to `true` to show a spinner. The `loadingAuto` prop auto-detects async `@click` handlers.
6. **Link buttons** — Use `to` (Nuxt router) or `href` (static URL) to render as an `<a>` tag.
