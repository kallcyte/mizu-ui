---
title: Avatar
description: A user image with fallback for initials or placeholder.
---

import AvatarDemo from "../../../../components/docs/AvatarDemo.vue";

<span style="display:inline-flex;margin-bottom:16px;padding:1px 8px;border-radius:9999px;font-size:11px;font-weight:500;background:#FFFBEB;color:#D97706;">Pending</span>

An avatar displays a user's photo, initials, or a placeholder. Mizu uses Nuxt UI's `UAvatar` with the default size scale (3xs-3xl).

## Examples

<AvatarDemo client:only="vue" />

## API

Reference: [Nuxt UI Avatar](https://ui.nuxt.com/docs/components/avatar)

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `'3xs' \| '2xs' \| 'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl' \| '2xl' \| '3xl'` | `'md'` | Avatar size |
| `src` | `string` | "” | Image source URL |
| `alt` | `string` | "” | Alt text for the image. Used to generate initials when no image is available |
| `text` | `string` | "” | Fallback text (overrides auto-generated initials) |
| `icon` | `string` (Iconify) | "” | Fallback icon when no image is available |
| `loading` | `boolean` | `false` | Show a loading skeleton while the image loads |

### Slots

| Slot | Props | Description |
|------|-------|-------------|
| `default` | "” | Default slot (overrides image, text, and icon) |

### Example Usage

```vue
<UAvatar src="https://i.pravatar.cc/80" alt="John Doe" />

<UAvatar alt="Jane Smith" text="JS" />

<UAvatar size="lg" icon="i-ph-user" />

<UAvatar :src="url" alt="User">
  <UIcon name="i-ph-user" />
</UAvatar>
```

## Theme

Nuxt UI v4 avatar defaults are already consistent with the Mizu design scale. The granular sizes (3xs-3xl, from 4px to 12px radius) match Mizu's icon and component sizing. No overrides are needed.

Default: `size="md"` (32px), `color="neutral"`.

## Anatomy

An avatar is a single element:

| Part | Required | Description |
|------|----------|-------------|
| Root | Yes | `<span>` with border-radius, overflow hidden, and background color |

When an image is provided, it renders an `<img>` inside. Otherwise, it shows fallback text or an icon.

## Implementation Notes

1. **Use `<UAvatar>` directly** "” No Mizu wrapper needed.
2. **Automatic initials** "” When `alt` is provided without `text` or `src`, Nuxt UI auto-generates initials from the alt text.
3. **Avatar groups** "” Use multiple `<UAvatar>` elements with negative margin stacking (`-ms-2`) for group layouts.
4. **Loading state** "” Set `loading` to `true` to show a skeleton placeholder while images load.
