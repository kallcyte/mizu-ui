# Release notes (scratch)

## MizuNavigationMenu — archived (2026-06-30)

The `MizuNavigationMenu` compound component (9 sub-components) was removed from the
build, package exports, docs sidebar, docs page, demo, and changelog, and archived to
`.agents/archive/navigation-menu/` for future re-introduction.

To restore it, see `.agents/archive/navigation-menu/README.md`.
